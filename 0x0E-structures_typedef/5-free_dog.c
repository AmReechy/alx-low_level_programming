#include "dog.h"
#include <stdlib.h>
#include <stddef.h>

/**
 * free_dog - a function that frees dogs
 * @d: pointer to the dog structure to be freed
 */

void free_dog(dog_t *d)
{
	free(dog_t->);
}
